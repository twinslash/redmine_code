<h2><%= t(:new_code) %></h2>
<%= labelled_form_for @code, :url => project_codes_path(@project) do |f| %>
	<%= render :partial => 'codes/form', :locals => { :f => f } %>
	<%= submit_tag l(:button_save) %>
<% end %>